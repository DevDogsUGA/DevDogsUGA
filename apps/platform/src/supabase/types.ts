/**
 * The shared, generated Supabase `Database` type lives in `@devdogsuga/supabase`
 * (regenerated via `pnpm --filter @devdogsuga/supabase generate-types`). This local re-export keeps the
 * historical `~/supabase/types` import path working across the app.
 */
export type { Database } from "@devdogsuga/supabase/types";
