/**
 * The shared, generated Supabase `Database` type lives in `@devdogsuga/sb`
 * (regenerated via `pnpm sb generate-types`). This local re-export keeps the
 * historical `~/supabase/types` import path working across the app.
 */
export type { Database } from "@devdogsuga/sb/types";
