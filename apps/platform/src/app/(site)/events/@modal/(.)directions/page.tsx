import FindUsContent from "~/components/EventsSection/FindUs/FindUsContent";

/** `/events/directions` when reached from the events page: the dialog's body. */
export default function DirectionsModalPage() {
  return <FindUsContent />;
}
