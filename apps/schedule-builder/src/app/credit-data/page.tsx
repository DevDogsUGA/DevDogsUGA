import { type Metadata } from "next";
import { ComingSoon } from "~/components/ComingSoon";

export const metadata: Metadata = { title: "Credit Data" };

export default function CreditDataPage() {
  return <ComingSoon title="Credit Data" />;
}
