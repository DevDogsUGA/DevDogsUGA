import { type Metadata } from "next";
import { ComingSoon } from "~/components/ComingSoon";

export const metadata: Metadata = { title: "Campus Distances" };

export default function DistanceMapPage() {
  return <ComingSoon title="Campus Distances" />;
}
