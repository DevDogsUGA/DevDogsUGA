import { type Metadata } from "next";
import { ComingSoon } from "~/components/ComingSoon";

export const metadata: Metadata = { title: "Route Map" };

export default function RouteMapPage() {
  return <ComingSoon title="Route Map" />;
}
