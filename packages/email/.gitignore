dist/
# Generated by scripts/compile.tsx on every build. Not tracked, because a
# committed copy would silently win over an out-of-date rebuild; the tracked
# artifact that makes design changes reviewable is __snapshots__/ instead.
src/generated/
