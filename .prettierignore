# format:check / format:write pass .gitignore as an --ignore-path too, so build
# output (dist, .next, .open-next, build, .turbo, coverage, drizzle-generated)
# is already excluded there. Only list files that are generated but still
# tracked — .gitignore structurally cannot express those.
**/src/database.types.ts
**/src/supabase/drizzle/
# Rewritten unformatted by every `db:pull`, so formatting it is a change that
# never sticks.
**/src/server/db/schema/generated/
